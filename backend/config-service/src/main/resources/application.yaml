server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/cristianromero97/APP_PEP2_TINGESO
          search-paths: "config-data"
  application:
    name: config-service
